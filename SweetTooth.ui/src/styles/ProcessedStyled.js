import styled from 'styled-components';

const ProcessedContainer = styled.div`
  margin: 10%;
`;

const ThankYouContainer = styled.div`
  font-size: 34px;
  font-weight: bold;
  color: #5e4c5a;
`;

const NumberContainer = styled.div`
  font-size: 25px;
  font-weight: bold;
  margin: 5%;
  color: #5e4c5a;
`;

export { ProcessedContainer, ThankYouContainer, NumberContainer };
